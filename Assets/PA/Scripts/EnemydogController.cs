using UnityEngine;

public class EnemydogController : MonoBehaviour
{
    private float normalDogSpeed = 8; //Geschwindigkeit Enemy (Julia)

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //Bewegung innerhalb Radiusbereich?

        //Attacke wenn Katze in Bereich kommt

        //Referenz für GameOver falls Katze erwischt
    }
}
