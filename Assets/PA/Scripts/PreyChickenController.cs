using UnityEngine;

public class PreyChickenController : MonoBehaviour
{
    private float normalChickenSpeed = 7; //normale Geschwindigkeit Prey (Julia)

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //Spawned random, Punkte beachten
        
        //Bewegung 

        //Chaosfaktor = schmeißt Boxen um bei Kollision

        //Flucht, wenn Katze zu nah ist

        //Referenz -> Tot wenn gefangen
    }
}
