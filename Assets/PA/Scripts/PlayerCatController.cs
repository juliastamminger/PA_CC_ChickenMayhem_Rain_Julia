using UnityEngine;

public class PlayerCatController : MonoBehaviour
{
    private float normalCatSpeed = 10; //normale Geschwindigkeit ohne Boost (Julia)

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //Bewegung via WASD                   (@Rain: wenn du einen Code geschrieben/verändert hast, schreib doch deinen Namen hinters Kommentar drüber. So ->)(Julia)
        
        //Springen?

        //Boost -> Referenz zu Timer

        //Referenz -> Punkte wenn Prey gefangen

        //GameGewonnen -> Timer leer + genug Prey gefangen

        //GameOver -> Referenz zu gefangen von Enemy oder Timer leer + zu wenig Prey gefangen

    }
}
